package main;

import javax.xml.bind.JAXBContext;
import javax.xml.bind.JAXBException;
import javax.xml.bind.Marshaller;
import javax.xml.bind.Unmarshaller;
import java.io.File;

public class Test {
    public static void main(String[] args) throws JAXBException {

    /*    Marshaller marshaller = jaxbContext.createMarshaller();
        if(file.length()<=10) {HighScoreFileHandling zero=new HighScoreFileHandling();
           zero.setClassicHighScore(0);
           zero.setArcadeHighScore(0);
            marshaller.marshal(zero,file);
        }
        HighScoreFileHandling save=new HighScoreFileHandling();
        save.setArcadeHighScore(50);
       marshaller.marshal(save,file);

       System.out.println(highscore.getArcadeHighScore());*/
    }
}
